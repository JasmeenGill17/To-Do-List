# To-Do-List
It is a To Do List application with interactive GUI designed in Python which keeps a log of tasks by perform-ing create, read, update and delete. Also it has data saving in 'json' file at backend.
